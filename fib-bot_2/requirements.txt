# Нужно только на VPS для реального запуска (load_fmp_daily + отправка в Telegram).
# Для локальных тестов (tests/test_pipeline.py, orchestrator.py на демо-данных)
# внешние пакеты не нужны -- используются только встроенные модули Python.
requests>=2.31
